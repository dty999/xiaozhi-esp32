# 小智AI机器人 代码模块依赖关系图

> 此图展示 `main/` 目录下各源码模块的依赖关系与职责

## 模块依赖总图

```mermaid
graph TD
    subgraph 入口["🚪 程序入口"]
        MAIN_CC["main.cc<br/>app_main()"]
    end

    subgraph 核心["⭐ 核心模块"]
        APP["application.cc/h<br/>Application 单例总管"]
        BOARD["board.cc/h<br/>Board 硬件抽象"]
        MCP["mcp_server.cc/h<br/>McpServer MCP工具注册"]
        ASSETS["assets.cc/h<br/>Assets 资源管理"]
        SETTINGS["settings.cc/h<br/>Settings NVS配置"]
        SYSINFO["system_info.cc/h<br/>SystemInfo 系统信息"]
    end

    subgraph 协议["📡 通信协议"]
        PROTO_ABS["protocols/protocol.h<br/>Protocol 抽象接口"]
        WS["protocols/websocket_protocol.cc<br/>WebsocketProtocol"]
        MQTT["protocols/mqtt_protocol.cc<br/>MqttProtocol"]
    end

    subgraph 音频["🔊 音频服务"]
        AUDIO_SVC["audio/audio_service.cc<br/>AudioService"]
        AUDIO_PROC["audio/audio_processor.cc<br/>AudioProcessor"]
        AUDIO_CODEC["audio/audio_codec.cc<br/>AudioCodec 抽象"]
    end

    subgraph 显示["🖥️ 显示系统"]
        DISPLAY_ABS["display/display.h<br/>Display 抽象接口"]
        LCD["display/lcd_display.cc<br/>LcdDisplay"]
        EMOTE["display/emote_display.cc<br/>EmoteDisplay"]
        LVGL["display/lvgl_*.cc<br/>LVGL 主题/控件"]
    end

    subgraph 硬件["🔩 硬件驱动"]
        LED["led/*.cc<br/>LED 驱动"]
        CODEC_CHIP["boards/*/audio_codec.cc<br/>各板卡音频芯片驱动"]
        DISP_CHIP["boards/*/display.cc<br/>各板卡显示驱动"]
    end

    subgraph 状态["🔄 状态管理"]
        STATE_ENUM["device_state.h<br/>DeviceState 枚举"]
        STATE_MACHINE["device_state_machine.cc<br/>DeviceStateMachine"]
        OTA["ota.cc/h<br/>Ota 空中升级"]
    end

    %% 入口依赖
    MAIN_CC --> APP

    %% 核心模块依赖
    APP --> BOARD
    APP --> MCP
    APP --> ASSETS
    APP --> SETTINGS
    APP --> AUDIO_SVC
    APP --> PROTO_ABS
    APP --> STATE_MACHINE
    APP --> OTA
    APP --> SYSINFO

    %% Board 依赖
    BOARD --> DISPLAY_ABS
    BOARD --> AUDIO_CODEC
    BOARD --> LED

    %% 协议依赖
    PROTO_ABS --> WS
    PROTO_ABS --> MQTT

    %% 显示依赖
    DISPLAY_ABS --> LCD
    DISPLAY_ABS --> EMOTE
    LCD --> LVGL

    %% 硬件依赖
    LCD --> DISP_CHIP
    AUDIO_CODEC --> CODEC_CHIP

    %% 状态依赖
    STATE_MACHINE --> STATE_ENUM
    OTA --> SETTINGS

    %% MCP依赖
    MCP --> BOARD
    MCP --> AUDIO_SVC
    MCP --> APP

    style 入口 fill:#FFE0B2,stroke:#E65100
    style 核心 fill:#E8F5E9,stroke:#2E7D32
    style 协议 fill:#BBDEFB,stroke:#1565C0
    style 音频 fill:#F3E5F5,stroke:#7B1FA2
    style 显示 fill:#FFF9C4,stroke:#F9A825
    style 硬件 fill:#FFCCBC,stroke:#D84315
    style 状态 fill:#B2DFDB,stroke:#00695C
```

## 模块职责一览

| 模块 | 文件 | 职责 |
|------|------|------|
| **main.cc** | `main/main.cc` | 程序入口：初始化 NVS，启动 Application |
| **Application** | `application.cc/h` | 全局总管：初始化、主循环、事件分派 |
| **Board** | `board.cc/h` | 硬件抽象：提供 GetDisplay/GetAudioCodec/GetLed/GetNetwork |
| **McpServer** | `mcp_server.cc/h` | MCP 工具注册与调用：AddTool + ParseMessage |
| **Assets** | `assets.cc/h` | 资源管理：Flash分区挂载，字体/emoji/皮肤加载与下载 |
| **Settings** | `settings.cc/h` | NVS 封装：键值对的读写，命名空间管理 |
| **Protocol** | `protocols/protocol.h` | 抽象通信接口：OpenAudioChannel/SendAudio/SendMcpMessage |
| **WebsocketProtocol** | `protocols/websocket_protocol.cc` | WebSocket 协议实现：wss 连接 + 音频流 |
| **MqttProtocol** | `protocols/mqtt_protocol.cc` | MQTT 协议实现：MQTT + UDP 音频传输 |
| **AudioService** | `audio/audio_service.cc` | 音频总管：采集、编码(OPUS)、解码、唤醒词(ESP-SR)、VAD |
| **AudioProcessor** | `audio/audio_processor.cc` | 音频处理引擎：编码/解码调度 |
| **Display** | `display/display.h` | 抽象显示接口：SetStatus/SetEmotion/SetChatMessage |
| **LcdDisplay** | `display/lcd_display.cc` | LCD/OLED 显示实现：基于 LVGL 图形库 |
| **EmoteDisplay** | `display/emote_display.cc` | 无屏版显示实现：表情通过 LED 矩阵 |
| **Ota** | `ota.cc/h` | OTA 升级：版本检查、下载、激活码 |
| **SystemInfo** | `system_info.cc/h` | 系统信息：版本号、UserAgent、内存统计 |

## 关键设计模式

```mermaid
flowchart LR
    subgraph 单例模式["单例 (Singleton)"]
        S1["Application::GetInstance()"]
        S2["Board::GetInstance()"]
        S3["McpServer::GetInstance()"]
        S4["Assets::GetInstance()"]
    end

    subgraph 策略模式["策略 (Strategy)"]
        ST1["Protocol<br/>← 抽象接口"]
        ST2["WebsocketProtocol<br/>← 策略A"]
        ST3["MqttProtocol<br/>← 策略B"]
    end

    subgraph 状态模式["状态 (State)"]
        SM1["DeviceStateMachine<br/>← 状态机核心"]
        SM2["HandleStateChangedEvent()<br/>← 各状态的行为"]
    end

    subgraph 观察者模式["观察者 (Observer)"]
        OB1["xEventGroupSetBits()<br/>← 生产者置铃"]
        OB2["xEventGroupWaitBits()<br/>← 消费者等铃"]
    end

    S1 --- S2 --- S3 --- S4
    ST1 --> ST2
    ST1 --> ST3
    SM1 --> SM2
    OB1 --> OB2
```

## 依赖层次

```
          main.cc
             │
      Application (应用层)
        ┌────┼────────┐
        │    │        │
    McpServer Board  Protocol
        │    ┌┼┐       │
        │   │ │ │  ┌───┴───┐
        │ Display │ WS     MQTT
        │   │   AudioService
        │   │       │
        └───┴───────┴────── 硬件驱动 (boards/)
```
