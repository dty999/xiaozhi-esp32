# 小智AI机器人 系统架构总图

> 此图展示项目四层架构：硬件层 → 框架层 → 应用层 → 服务端

## 整体架构层次

```mermaid
graph TB
    subgraph 服务端["☁️ 服务端 (xiaozhi.me / 自建)"]
        SRV_ASR["语音识别 ASR<br/>Whisper / FunASR"]
        SRV_LLM["大语言模型 LLM<br/>Qwen / DeepSeek"]
        SRV_TTS["语音合成 TTS<br/>CosyVoice / MeloTTS"]
        SRV_MCP[("MCP 工具调度<br/>IoT控制 / 知识搜索")]
        SRV_CTL["设备管理与配网"]
    end

    subgraph 应用层["📱 应用层 Application (本项目核心)"]
        APP["Application<br/>单例总管"]
        STATE["DeviceStateMachine<br/>状态机"]
        PROTO["Protocol<br/>通信协议"]
        MCP["McpServer<br/>MCP工具注册"]
        ASSETS["Assets<br/>资源管理"]
        SETTINGS["Settings<br/>配置存取(NVS)"]
    end

    subgraph 框架层["🔧 ESP-IDF 框架层 (乐鑫官方)"]
        FREERTOS["FreeRTOS<br/>实时操作系统"]
        EVENT["Event Groups<br/>事件组(13铃)"]
        ESP_TIMER["esp_timer<br/>硬件定时器"]
        WIFI["Wi-Fi / 4G<br/>网络协议栈"]
        GPIO["GPIO / I2C / SPI<br/>外设驱动"]
        NVS["NVS Flash<br/>非易失存储"]
        ESP_SR["ESP-SR<br/>语音唤醒"]
    end

    subgraph 硬件层["🖥️ 硬件层 (ESP32芯片 + 外设)"]
        MCU["ESP32-S3 / C3 / P4<br/>Xtensa/RISC-V 双核"]
        MIC["麦克风<br/>I2S 数字硅麦"]
        SPK["扬声器<br/>DAC/I2S功放"]
        LCD["显示屏<br/>LCD / OLED / AMOLED"]
        LED["LED 指示灯<br/>GPIO / WS2812"]
        BTN["按键<br/>GPIO 物理按键"]
        BAT["电池<br/>ADC 电量采集"]
    end

    服务端 -->|"WebSocket / MQTT"| PROTO
    PROTO --> APP
    APP --> STATE
    APP --> MCP
    APP --> ASSETS
    APP --> SETTINGS
    STATE --> APP
    MCP --> PROTO

    APP --> FREERTOS
    APP --> EVENT
    APP --> ESP_TIMER
    PROTO --> WIFI
    MCP --> GPIO
    SETTINGS --> NVS
    ASSETS --> NVS

    FREERTOS --> MCU
    EVENT --> FREERTOS
    ESP_TIMER --> MCU
    WIFI --> MCU
    GPIO --> MCU
    NVS --> MCU
    ESP_SR --> MCU

    MCU --> MIC
    MCU --> SPK
    MCU --> LCD
    GPIO --> LED
    GPIO --> BTN
    MCU --> BAT

    style 服务端 fill:#E3F2FD,stroke:#1565C0
    style 应用层 fill:#E8F5E9,stroke:#2E7D32
    style 框架层 fill:#FFF3E0,stroke:#E65100
    style 硬件层 fill:#FCE4EC,stroke:#C62828
```

## 各层职责

| 层次 | 文件/组件 | 职责 |
|------|-----------|------|
| **硬件层** | ESP32芯片、麦克风、喇叭、屏幕 | 物理世界的输入输出 |
| **框架层** | FreeRTOS、ESP-IDF API | 提供操作系统能力、硬件抽象 |
| **应用层** | `application.cc`、`mcp_server.cc` 等 | 实现小智具体逻辑：状态管理、通信、设备控制 |
| **服务端** | xiaozhi.me 或自建服务器 | AI 处理：语音识别、大模型推理、语音合成 |
